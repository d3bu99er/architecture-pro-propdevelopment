apiVersion: v1
kind: Pod
metadata:
  name: pod-root-user
  namespace: audit-zone
spec:
  containers:
    - name: nginx
      image: nginxinc/nginx-unprivileged
      securityContext:
        runAsUser: 0