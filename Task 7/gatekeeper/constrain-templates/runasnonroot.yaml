apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: k8srequirerunasnonroot
spec:
  crd:
    spec:
      names:
        kind: K8sRequireRunAsNonRoot
      validation:
        openAPIV3Schema:
          type: object
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
        package k8srequirerunasnonroot

        violation[{"msg": msg}] {
          c := input.review.object.spec.containers[_]
          not c.securityContext.runAsNonRoot
          msg := sprintf("runAsNonRoot=true is required for container %q", [c.name])
        }

        violation[{"msg": msg}] {
          c := input.review.object.spec.initContainers[_]
          not c.securityContext.runAsNonRoot
          msg := sprintf("runAsNonRoot=true is required for initContainer %q", [c.name])
        }

        violation[{"msg": msg}] {
          c := input.review.object.spec.ephemeralContainers[_]
          not c.securityContext.runAsNonRoot
          msg := sprintf("runAsNonRoot=true is required for ephemeralContainer %q", [c.name])
        }
